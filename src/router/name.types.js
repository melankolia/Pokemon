const home = "home";
export const HOME = {
    ROOT: `${home}`
};

const about = "about";
export const ABOUT = {
    ROOT: `${about}`
};

const detail = "detail";
export const DETAIL = {
    ROOT: `${detail}`
};