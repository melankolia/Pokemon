const home = "home";
export const HOME = {
    ROOT: `${home}`
};

const pokedex = "pokedex";
export const POKEDEX = {
    ROOT: `${pokedex}`
};

const detail = "detail";
export const DETAIL = {
    ROOT: `${detail}`
};